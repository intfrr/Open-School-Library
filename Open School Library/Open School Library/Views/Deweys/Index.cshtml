@model IEnumerable<Open_School_Library.Models.Dewey>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Dewey Decimal
        </th>
        <th>
            Class
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeweyNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeweyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeweyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeweyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeweyID })
        </td>
    </tr>
}

</table>
